/** This file defines the types used in the search bar. */

export enum CategoryType {
  NUMBER = "number",
  STRING = "string",
  BOOLEAN = "boolean",
  DATE = "date",
  CUSTOM = "custom",
  UNKNOWN = "unknown",
}
